import 'package:flutter/material.dart';

class SmeupSplash extends StatelessWidget {
  final Color color;
  SmeupSplash(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}
